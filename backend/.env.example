# Backend configuration (copy to .env and fill in as needed).

# Optional: override examples directory location.
# EXAMPLES_DIR=/app/examples

# Optional: LLM provider keys for future natural-language command parser.
# Leave blank to use the deterministic rule-based parser (no key required).
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
