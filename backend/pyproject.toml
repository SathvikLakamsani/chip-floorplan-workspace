[project]
name = "chip-floorplan-workspace-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.109.0",
  "uvicorn[standard]>=0.27.0",
  "pydantic>=2.5.0",
  "python-multipart>=0.0.6",
]

[tool.vercel]
entrypoint = "app.main:app"
